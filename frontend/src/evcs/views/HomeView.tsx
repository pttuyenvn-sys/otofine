import { HomeHero } from "@evcs/components/home/HomeHero";

/**
 * Sprint 03A — Above The Fold only (Hero + KPI).
 * Các section tiếp theo: Sprint 03B+.
 */
export function HomeView() {
  return <HomeHero />;
}
